class vcrMainProcessor {
	// handle commands
}

class vcrSettingsProcessor {
	// handle settings (get/set)
}

class vcrTimeProcessor {
	// handle time (curdate and tape time)
}

class vcrDisplayProcessor {
	// handle lcd display (info + time)
}

class vcrSoundProcessor {
	// handle vcr sounds
}

class vcrTapeProcessor {
	// handle vhs tape from insert to eject
}

class vcrPlaybackProcessor {
	// handle playback (play, pause, slowmo, fwd)
}

class vcrSignalProcessor {
	// handle vcr signal (what is generated and shown)
}
